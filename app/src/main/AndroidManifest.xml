<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cobeosijek.to_dolist">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.to_do_list.ToDoListActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.add_to_do.AddToDoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.details_to_do.DetailsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.edit_to_do.EditToDoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.search_to_do.SearchActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>